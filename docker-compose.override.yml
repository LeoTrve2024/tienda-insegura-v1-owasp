services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"

  backend:
    ports:
      - "127.0.0.1:8080:8080"

  frontend:
    ports:
      - "127.0.0.1:3000:80"

networks:
  laboratorio:
    internal: false
